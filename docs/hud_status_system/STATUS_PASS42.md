# Pass 42 - Field Status/Menu Stage04 Pointer/Layout Core 100%

Escopo fechado em 100%:

- documentado o stage `$95 = $04` apos o bootstrap visual;
- renomeados os blocos `CODE_82DC0D` ate `CODE_82DD80` do stage `$04`;
- documentada a espera de `$3C` frames e o scroll de BG2;
- documentada a matriz Pointer42 para slots `$04-$08`;
- documentada a ramificacao por `$098D` em tres variantes;
- mantido comportamento byte-perfect.

Proxima meta recomendada: mapear o stage `$95 = $05`, que usa `$97` e atualiza os ponteiros de layout em runtime.
