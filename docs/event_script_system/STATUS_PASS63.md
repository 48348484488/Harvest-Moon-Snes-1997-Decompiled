# STATUS PASS 63

Closed in Pass 63:

- Global `DATA8_` generic labels in `project_buildable/src`: 121 -> 0.
- Global `DATA8_` generic labels in `source_decompilada/src`: 121 -> 0.
- `SUB_`, `DATA16_`, and `UNK_` remain closed at 0.
- Rebuild remains byte-perfect with MD5 `c9bf36a816b6d54aed79d43a6c45111a`.

Still open after Pass 63:

- `CODE_` generic labels in `project_buildable/src`: 2322 unique / 6101 occurrences.
- Individual EventScript scene naming and unknown opcode interpretation.
- NPC/personality/event mapping and sprite/GOBJ semantic mapping.
