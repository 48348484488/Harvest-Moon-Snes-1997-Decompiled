# STATUS PASS 29 — Event Script / Scenario Core 100%

Escopo fechado: núcleo do interpretador de eventos/scripts.

Incluído nesta pass:

- slots de execução de eventos;
- carregamento de ponteiro de script;
- loop de atualização dos scripts ativos;
- dispatch por opcode;
- comandos de tempo, player, transição, fade, jumps, flags, texto, áudio, mapa, item e animais;
- pseudocódigo do interpretador;
- renomeações seguras no banco `84`.

Fora do escopo:

- catalogar todos os scripts individuais dos bancos `B3-B5` cena por cena;
- nomear todos os eventos específicos de NPC;
- reconstruir uma linguagem/assembler próprio de evento.
