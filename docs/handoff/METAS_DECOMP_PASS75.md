# METAS DECOMP PASS75

Concluido:

- Classificar os 99 residuos in-group da Pass 74.
- Fechar os 73 residuos `$B4` como payload inline tile/objeto.
- Reduzir o alvo real para 26 residuos non-B4.
- Manter o rebuild byte-perfect.

Proxima meta recomendada:

1. Trace manual dos grupos `$00` e `$01`, que concentram 12 dos 26 residuos restantes.
2. Depois fechar `$24` e `$43`, com 4 residuos cada.
3. Por fim fechar os grupos unitarios `$02`, `$03`, `$09`, `$15` e os dois residuos de `$0B`.
