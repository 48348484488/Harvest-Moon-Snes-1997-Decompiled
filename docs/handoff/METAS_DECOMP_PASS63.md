# Handoff - Pass 63

Recommended next pass:

1. Target one `CODE_` cluster with high occurrence count instead of random global replacements.
2. Good candidates: Bank 84 EventCmd aliases or Bank 81 player-action dispatch clusters.
3. Keep every pass label-only/comment-only unless a fully validated semantic decode is available.
4. Rebuild after every batch and preserve the NO-ROM package rule.
