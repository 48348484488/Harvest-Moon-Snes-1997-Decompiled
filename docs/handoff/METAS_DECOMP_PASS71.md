# Metas apos Pass 71

Fechado em 100% nesta pass: EventCmd oficial `$51-$59` e cobertura completa da tabela `EventInstructionPointers` `$00-$59`.

Proximos alvos para continuar rumo ao 100% humano:

1. Separar bytes invalidos `>$59` entre dados inline, ponteiros, tabelas e reais limites de script.
2. Nomear entradas de EventScript por cena/personagem/festival.
3. Cruzar grupos com `UnlinkedText.txt` para mapear dialogs individuais sem despejar texto no relatorio.
4. Associar GOBJ/sprite pointers dos comandos `$1A/$2B/$2D/$2E/$49` com objetos reais.
