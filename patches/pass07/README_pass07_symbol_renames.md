# Pass 07 symbol renames

This folder records the semantic symbol renames applied in Pass 07. The source was rebuilt after these changes and remained byte-perfect.

See `pass07_item_symbol_rename_map.csv` for the complete mapping.
